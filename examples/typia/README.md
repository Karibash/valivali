# valivali-typia-example

This is a example program describing the basic usage of valivali packages with the typia.

## Usage

The example program can be run by executing the following command.

```sh
# install dependencies
$ yarn install

# run example
$ yarn run:typia
```
